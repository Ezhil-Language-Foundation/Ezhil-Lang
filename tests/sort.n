# (C) முத்தையா அண்ணாமலை 2013
# இது ஒரு எழில் தமிழ் நிரலாக்க மொழி உதாரணம்

## sort descending 
நிரல்பாகம்  sort ( n )
  i = 0;
  @( i < len(n) ) வரை
     j = i;
     @( j < len(n) ) வரை
        @( n(j) > n(i) ) ஆனால்
               tmp = n(j);
               n(j) = n(i);
               n(i) = tmp;
                    முடி
            முடி
     முடி

    பின்கொடு  n;
முடி


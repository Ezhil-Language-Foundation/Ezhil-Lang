x = 1

while ( x < 10 )
      x = x + 1

      if ( (x % 2 ) == 0 )
	 continue ## skip even case.
      end

      print "" ## check if Lexer works OK. it Does
      if ( ( x % 2 ) == 0 )
         print "Even Number", x
      else
         print "Odd Number", x
      end
end

# Ref: http://ta.wikipedia.org/wiki/பைத்தான்
# comment விளக்கம் கூறு
# வெளியீடு(Output)
பதிப்பி  "வருக வையகமே!"
a = 1
b = 2
பதிப்பி a + b b

#comment விளக்கம் கூறு
# வெளியீடு(Output)
a = 1
b =  2
language = "python"
பதிப்பி a, b, language

# கூற்று(Expression)
பதிப்பி 1 + 2, 2 - 3, 2 * 3, 5 / 2, 5.0 / 2

# சரம்(String)
language = "தமிழ்"
lang = "தமிழ்"
l = "தமிழ்"
kural = "அகர முதல எழுத்தெல்லாம் ஆதி \n பகவன் முதற்றே உலகு."

பதிப்பி language, lang, l
பதிப்பி kural

# கட்டியம்(Condition)
a = 23
@( a > 0 ) ஆனால்
	printf( "a %d is positive number" ,a)
இல்லை
	printf( "a %d is negative number " ,a)
முடி

a = 0
@( a > 0 ) ஆனால்
	printf("a(%d) is positive number",a)
@( a < 0 ) இல்லைஆனால்
	printf("a(%d) is negative number",a)
@( a == 0) இல்லை
	printf("a(%d) is zero",a)
முடி

# அகராதி(Dictionary)
d = {"name": "Tamil Magan", "age": 23, "place": "coimbatore"}
பதிப்பி d
பதிப்பி d["name"]
பதிப்பி d["age"]
பதிப்பி keys(d)
பதிப்பி values(d)
பதிப்பி items(d)

# முழுச்சுற்றுகள் தொடர்/டியுப்பல்
@( [1, 2, 3, 4, 5] இல் x) ஒவ்வொன்றாக
    பதிப்பி(x)
முடி

# அகராதி(Dictionary)
d = {"age": 23, "name": "Tamil Magan", "place": "coimbatore"}
@( keys(d) இல் key) ஒவ்வொன்றாக
    பதிப்பி key, d[key]
முடி

# ஒயில் (while)
no = 1
@( no < 10 ) வரை
    @( (no % 2) == 0 ) ஆனால்
        பதிப்பி no, "இரட்டை"
    இல்லை
        பதிப்பி no, "ஒற்றை"
    முடி
    no = no + 1
முடி
பதிப்பி no

நிரல்பாகம் square(no)
    பின்கொடு (no * no)
முடி


நிரல்பாகம் cube(no)
    பின்கொடு (no *square(no))
முடி
 

 
printf("%g", square(2))
printf("%g", cube(square(4)) )

# assigning functions not OK yet
# triple = cube;
# printf("%g", triple((4)) )

@( "மேற்கோள்கள்" இல் x) ஒவ்வொன்றாக
   பதிப்பி x
முடி
